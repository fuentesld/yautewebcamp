<%- include('./includes/head.ejs') %>
</head>
<body>

  <%- include('./includes/navegacion.ejs') %>

  <!-- * REGISTRO DE USUARIOS -->
  <section class="seccion contenedor">
    <h2>Resumen del Registro</h2>
    <h3>Hola <%= datos.nombre_registrado %>:</h3>
    <p>Gracias por registrarte a YauteWebCamp 2020, enviaremos un correo a <%= datos.email_registrado %> con la siguiente informacion para su validación.</p>
    <p>Clave de Registro: <%= datos.id_registrado %></p>
    <p>Nombre: <%= datos.nombre_registrado %>  <%= datos.apellido_registrado %></p>
    <% if (datos.compra_registrado.undia) {%><p>Pase(s) un dia: <%= datos.compra_registrado.undia %></p>
      <%}%>
    <% if (datos.compra_registrado.dosDias > 0) {%><p>Pase(s) 2 dias: <%= datos.compra_registrado.dosDias %></p>
      <%}%>
    <% if (datos.compra_registrado.completo>0) {%><p>Pase(s) 3 dias: <%= datos.compra_registrado.completo %></p>
      <%}%>

    <% if (datos.compra_registrado.camisas > 0) {%>
      <p>Camisas conmemorativas YauteWebCamp2020: <%=datos.compra_registrado.camisas%></p>
    <%}%>
    <% if (datos.compra_registrado.etiquetas > 0) {%>
      <p>Etiquetas del evento (HTML, CSS, JavaScript, Chrome): <%=datos.compra_registrado.etiquetas%></p>
    <%}%>

    <p>Total a Pagar: <%= datos.totalpagado_registrado%></p>
  </section>

    <%- include('./includes/footer.ejs') %>
  
    <!-- <script type="text/javascript" src="js/registro.js"></script> -->
  </body>
  </html>